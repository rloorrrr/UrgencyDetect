# === Core numeric / data ===
numpy==2.0.2
pandas==2.2.2
pyarrow==16.1.0
scikit-learn==1.5.2
tqdm>=4.66.4

# === PyTorch / training ===
# CUDA 환경마다 torch 휠이 달라 충돌을 피하려고 버전 고정은 하지 않습니다.
# (로컬/서버 CUDA에 맞춰 별도 설치 권장: pip install torch --index-url https://download.pytorch.org/whl/cu121 등)
torch

# === Hugging Face stack ===
transformers>=4.44.0
accelerate>=0.33.0
